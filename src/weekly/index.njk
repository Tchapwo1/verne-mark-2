---
layout: layouts/base.njk
title: Weekly Intel
permalink: /weekly/
---

<header class="mb-12 border-b border-gray-200 pb-8">
  <h1 class="text-4xl font-extrabold tracking-tight text-slate-900 mb-4">Weekly Intel</h1>
  <p class="text-xl text-slate-600">Curated intelligence and strategic insights for the week ahead.</p>
</header>

<div class="grid grid-cols-1 md:grid-cols-2 gap-x-12 gap-y-16">
  {%- for post in collections.weekly | reverse -%}
  <article class="flex flex-col group h-full">
    <time class="text-xs font-semibold text-blue-600 uppercase tracking-widest mb-3" datetime="{{ post.date }}">{{ post.date | readableDate }}</time>
    <a href="{{ post.url }}" class="block flex-grow">
      <h2 class="text-3xl font-bold text-slate-900 group-hover:text-blue-600 transition-colors mb-4 leading-tight">
        {{ post.data.title }}
      </h2>
      {% if post.data.description %}
      <p class="text-slate-600 mb-4 leading-relaxed line-clamp-3">{{ post.data.description }}</p>
      {% endif %}
    </a>
    <div class="mt-4 pt-4 border-t border-gray-100">
      <a href="{{ post.url }}" class="text-sm font-semibold text-slate-900 group-hover:text-blue-600">Read Intelligence &rarr;</a>
    </div>
  </article>
  {%- endfor -%}
</div>
