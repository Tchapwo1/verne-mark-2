---
layout: layouts/base.njk
title: Daily Analysis
permalink: /daily/
---

<header class="mb-12 border-b border-gray-200 dark:border-slate-800 pb-8 text-center md:text-left">
  <h1 class="text-4xl md:text-5xl font-extrabold tracking-tight text-slate-900 dark:text-white mb-4 font-serif">Daily Analysis</h1>
  <p class="text-xl text-slate-600 dark:text-slate-400 max-w-2xl mb-8">Fresh perspectives and deep dives, delivered every day.</p>
  
  <!-- Frequency Heatmap (Last 60 Days) -->
  <div class="flex flex-col md:flex-row items-center gap-4">
    <div class="flex gap-1 flex-wrap max-w-lg justify-center md:justify-start" title="Publishing Frequency (Last 60 Days)">
      {% for day in collections.daily | getDailyActivity %}
        <div 
          class="w-3 h-3 rounded-sm transition-all hover:scale-125 {{ 'bg-blue-600 dark:bg-blue-500 shadow-sm shadow-blue-500/50' if day.count > 0 else 'bg-slate-200 dark:bg-slate-800' }}"
          title="{{ day.date }}: {{ day.count }} Post{{ 's' if day.count != 1 else '' }}"
        ></div>
      {% endfor %}
    </div>
    <span class="text-xs text-slate-400 uppercase tracking-widest font-bold">Latest 60 Days</span>
  </div>
</header>

<div class="space-y-12">
  {%- for post in collections.daily | reverse -%}
  <article class="group grid grid-cols-1 md:grid-cols-4 gap-6 items-baseline">
    <div class="md:col-span-1">
      <time class="text-sm font-semibold text-slate-400 uppercase tracking-wider" datetime="{{ post.date }}">{{ post.date | readableDate }}</time>
    </div>
    <div class="md:col-span-3">
      <a href="{{ post.url }}" class="block">
        <h2 class="text-2xl font-bold text-slate-900 group-hover:text-blue-600 transition-colors mb-2">
          {{ post.data.title }}
        </h2>
        {% if post.data.description %}
        <p class="text-slate-600 mb-3 leading-relaxed">{{ post.data.description }}</p>
        {% endif %}
        <span class="text-blue-600 font-medium text-sm group-hover:underline">Read article &rarr;</span>
      </a>
    </div>
  </article>
  {%- endfor -%}
</div>
